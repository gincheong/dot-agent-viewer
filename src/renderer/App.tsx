// Phase A scaffold — hello world placeholder.
// Phase C will replace this with the full two-pane layout (Sidebar + DetailPane).
export default function App(): JSX.Element {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100vh',
        fontFamily: 'system-ui, -apple-system, sans-serif',
        color: '#1a1a1a',
      }}
    >
      dot-agent-viewer — Phase A scaffold
    </div>
  )
}
